#set page(width: 11in, height: 8.5in, margin: 0.45in)
#set par(justify: false)

#align(center)[
  #text(size: 16pt, weight: "bold")[Universal Derivative Filter REV1]
  #linebreak()
  #text(size: 11pt)[Manual Transcription]
]

#v(0.8em)

This document typesets the manually reviewed transcription stored in
`universal_derivative_filter_REV1_manual_transcription.txt`.
Uncertain or illegible text is preserved as `[unclear]` or `[illegible]`.

#v(0.8em)

#raw(read("universal_derivative_filter_REV1_manual_transcription.txt"), block: true)
